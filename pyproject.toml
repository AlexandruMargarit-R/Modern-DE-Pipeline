[project]
name = "modern-pipeline-personal-project"
version = "0.1.0"
description = "A modern data engineering pipeline project with free and open-source tools."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "kafka-python>=3.0.0",
]
